#include "lists.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * reverse_listint - reverse list
 * @head: ptr head first node param
 *
 * Return: return
 */
listint_t *reverse_listint(listint_t **head)
{
	listint_t *next, *prev;
*head = h
*perv = p
p = NULL;
w
